<div class="aroma-membership-thankyou">
  <div class="thankyou-container">
    <div class="thankyou-content">
      <div class="success-icon">✓</div>
      <h1 class="thankyou-title">{{i18n "aroma_membership.thankyou_title"}}</h1>
      <p class="thankyou-message">{{i18n "aroma_membership.thankyou_message"}}</p>
      
      {{#if this.model.orderId}}
        <div class="order-info">
          <p class="order-label">{{i18n "aroma_membership.order_number"}}:</p>
          <p class="order-id">{{this.model.orderId}}</p>
        </div>
      {{/if}}
      
      <div class="next-steps">
        <h3 class="steps-title">{{i18n "aroma_membership.next_steps"}}</h3>
        <ul class="steps-list">
          <li class="step-item">
            <span class="step-icon">📧</span>
            <span class="step-text">{{i18n "aroma_membership.step_email"}}</span>
          </li>
          <li class="step-item">
            <span class="step-icon">🔐</span>
            <span class="step-text">{{i18n "aroma_membership.step_account"}}</span>
          </li>
          <li class="step-item">
            <span class="step-icon">🎉</span>
            <span class="step-text">{{i18n "aroma_membership.step_enjoy"}}</span>
          </li>
        </ul>
      </div>
      
      <div class="thankyou-actions">
        <a href="/" class="btn btn-primary btn-large">
          {{i18n "aroma_membership.back_to_home"}}
        </a>
        <a href="/u/{{this.currentUser.username}}" class="btn btn-secondary">
          {{i18n "aroma_membership.go_to_profile"}}
        </a>
      </div>
    </div>
  </div>
</div>

